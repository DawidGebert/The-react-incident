import { Container, List, ListItem, Typography } from '@mui/material';
import React from 'react';

const Spells = ({ setCurrentId }) => {
  return (
    <Container maxWidth="lg">
      <Typography variant='h5'>pusto...</Typography>
    </Container>
  );
};

export default Spells;
